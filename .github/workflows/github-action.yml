name: My Workflow

on:
  # Runs when manually triggered
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: 23f1001691@ds.study.iitm.ac.in
        run: echo "Hello, world!"
